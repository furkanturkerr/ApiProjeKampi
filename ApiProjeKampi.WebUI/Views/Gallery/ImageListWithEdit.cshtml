@model List<ApiProjeKampi.WebUI.Dtos.ImageDtos.ResultImageDto>

@{
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<section class="section">
    <div class="section-body">
        <div class="row">
            <div class="col-12 col-md-12 col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Kategori Listesi</h4>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Görsel Başlığı</th>
                                <th scope="col">Sil</th>
                                <th scope="col">Güncelle</th>
                            </tr>
                            </thead>
                            <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.Title</td>
                                    <td><a href="~/Gallery/DeleteImage/@item.ImageId" class="btn btn-danger">Sil</a></td>
                                    <td><a href="~/Gallery/UpdateImage/@item.ImageId" class="btn btn-warning">Güncelle</a></td>
                                </tr>
                            }
                            </tbody>
                        </table>
                        <a href="~/Gallery/CreateImage" class="btn btn-info">Yeni Görsel Ekle</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>